[build]
  # Build command
  command = "pnpm build"

  # Directory to publish (Next.js outputs to .next by default)
  publish = ".next"

[build.environment]
  # Node version
  NODE_VERSION = "20"

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200